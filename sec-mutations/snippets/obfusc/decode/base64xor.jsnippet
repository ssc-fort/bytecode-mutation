byte[] data = java.util.Base64.getDecoder().decode("#STRING#");
for (int i = 0; i < data.length; i++) {
	data[i] ^= #BYTES#[i % #BYTES#.length];
}
String text = new String(data, java.nio.charset.StandardCharsets.UTF_8);
