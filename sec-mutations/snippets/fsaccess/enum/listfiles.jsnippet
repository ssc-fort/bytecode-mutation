java.io.File dir = new java.io.File("#FILEPATH#");
java.io.File[] files = dir.listFiles();
java.util.List hits = new java.util.ArrayList();
if (files != null) {
	for (int i = 0; i < files.length; i++) {
		if (files[i].getName().endsWith("#STRING#")) {
			hits.add(files[i]);
		}
	}
}
